; PHP overrides for STRATO webhosting (per-directory, applied by PHP-FastCGI).
; Allow the 15 MB font uploads the submit form accepts. post_max_size must be
; a little larger than upload_max_filesize to leave room for the other fields.
upload_max_filesize = 16M
post_max_size = 20M
